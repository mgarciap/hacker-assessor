<%= form_for([@requirement.career, @requirement]) do |f| %>
  <% if @requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requirement.errors.count, "error") %> prohibited this requirement from being saved:</h2>

      <ul>
      <% @requirement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :level %><br>
    <%= f.number_field :level %>
  </div>
  <div class="field">
    <%= f.label :seniority %><br>
    <%= f.text_field :seniority %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.skill_select %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
