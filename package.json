{
  "name": "hacker-assessor",
  "devDependencies": {
    "gulp": "^3.8.10",
    "gulp-inject": "^1.0.2",
    "main-bower-files": "^2.4.1"
  },
  "scripts": {
    "start": "browser-sync start --files 'public/*.html, public/*.js' --server 'public'",
    "test": "protractor test/config.js"
  }
}
